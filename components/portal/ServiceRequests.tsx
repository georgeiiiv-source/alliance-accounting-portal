"use client";
import { useState } from "react";import { ArrowRight } from "lucide-react";
const kinds=['Payroll service','Sales tax service','Entity formation','IRS notice help','Bookkeeping consultation'];
export function ServiceRequests({requests}:{requests:{id:string;kind:string;status:string;createdAt:string}[]}){const[notice,setNotice]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const res=await fetch('/api/service-requests',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});if(res.ok){setNotice('Request submitted securely.');setTimeout(()=>location.reload(),500)}}return <div className="portal-content"><div className="welcome"><div><h1>Service requests</h1><span>Request remote accounting, payroll, entity, or IRS notice support.</span></div></div><form className="contact-form" onSubmit={submit}><select name="kind" required defaultValue=""><option value="" disabled>Select a service</option>{kinds.map(k=><option key={k}>{k}</option>)}</select><textarea name="details" required placeholder="Describe how we can help"/><button className="btn">Send secure request <ArrowRight/></button>{notice&&<p>{notice}</p>}</form><section className="panel files-panel"><div className="panel-head"><div><small>REQUEST HISTORY</small><h3>Your requests</h3></div></div>{requests.map(r=><div className="activity" key={r.id}><span><b>{r.kind}</b><small>{new Date(r.createdAt).toLocaleDateString()} · {r.status}</small></span></div>)}{!requests.length&&<div className="empty-state">No service requests yet.</div>}</section></div>}
