"use client";
import { useState } from "react";
import { ShieldCheck, Upload } from "lucide-react";
const categories=['W-2','1099','Bank statements','Business expenses','ID documents','Prior tax return','IRS letters','Payroll documents','Bookkeeping documents'];
export function FileUpload(){const[progress,setProgress]=useState(0);const[message,setMessage]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setProgress(15);const data=new FormData(e.currentTarget);setProgress(45);const res=await fetch('/api/documents',{method:'POST',body:data});setProgress(res.ok?100:0);setMessage(res.ok?'Document queued for secure upload and virus scanning.':'Upload could not be created.');if(res.ok)setTimeout(()=>location.reload(),500)}return <form className="upload-box" onSubmit={submit}><div className="upload-icon"><Upload/></div><h3>Secure file upload</h3><p>PDF, JPG, PNG, DOCX, XLSX or CSV · 25 MB maximum</p><select name="category" required defaultValue=""><option value="" disabled>Select document category</option>{categories.map(c=><option key={c}>{c}</option>)}</select><input name="file" type="file" required accept=".pdf,.jpg,.jpeg,.png,.docx,.xlsx,.csv"/><button className="btn" type="submit"><Upload/> Upload document</button>{progress>0&&<div className="upload-progress"><i style={{width:`${progress}%`}}/></div>}<small><ShieldCheck/> Every file is restricted, scanned, and encrypted</small>{message&&<p role="status">{message}</p>}</form>}
