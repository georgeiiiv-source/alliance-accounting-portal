"use client";
import { useState } from "react";import Link from "next/link";import { ArrowRight } from "lucide-react";import { Logo } from "@/components/layout/Logo";
export function ForgotPasswordPage({token}:{token?:string}){const[msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const endpoint=token?'/api/auth/reset-password':'/api/auth/forgot-password';const res=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...Object.fromEntries(form),token})});const data=await res.json();setMsg(res.ok?data.message:data.error)}return <div className="login-page"><div className="login-side"><Logo light/><div><h1>{token?'Choose a new password.':'Secure account recovery.'}</h1><p>Recovery links expire after one hour and can only be used once.</p></div></div><div className="login-form"><div className="form-wrap"><h2>{token?'Reset password':'Forgot password'}</h2><form onSubmit={submit}>{token?<label>New password<input name="password" type="password" required minLength={12}/></label>:<label>Email address<input name="email" type="email" required/></label>}<button className="btn">{token?'Update password':'Send reset link'} <ArrowRight/></button></form>{msg&&<p role="status">{msg}</p>}<div className="switch"><Link href="/login">Return to sign in</Link></div></div></div></div>}
