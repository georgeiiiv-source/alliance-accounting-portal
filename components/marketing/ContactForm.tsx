"use client";
import { useState } from "react";
import { ArrowRight } from "lucide-react";
export function ContactForm(){const[sent,setSent]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const res=await fetch('/api/service-requests',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});setSent(res.ok)}return <form className="contact-form" onSubmit={submit}><input name="name" required placeholder="Your name"/><input name="email" required type="email" placeholder="Email address"/><select name="kind" required defaultValue=""><option value="" disabled>How can we help?</option><option>Tax preparation</option><option>Bookkeeping</option><option>Advisory</option></select><textarea name="details" required placeholder="Tell us a little about your needs"/><button className="btn" type="submit">{sent?'Inquiry received':'Send inquiry'} <ArrowRight/></button></form>}
