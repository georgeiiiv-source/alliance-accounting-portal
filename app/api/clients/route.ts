import { auth } from "@/auth";import { prisma } from "@/lib/prisma";import { isStaff,jsonError } from "@/lib/api";import { NextResponse } from "next/server";
export async function GET(request:Request){const session=await auth();if(!isStaff(session))return jsonError('Forbidden',403);const clients=await prisma.user.findMany({where:{role:'CLIENT'},include:{profile:true},orderBy:{createdAt:'desc'}});if(new URL(request.url).searchParams.get('format')==='csv'){const rows=['name,email,phone,tax_year,filing_type',...clients.map(c=>[c.profile?.fullName??c.name,c.email,c.profile?.phone,c.profile?.taxYear,c.profile?.filingType].map(v=>`"${String(v??'').replaceAll('"','""')}"`).join(','))];return new Response(rows.join('\n'),{headers:{'content-type':'text/csv','content-disposition':'attachment; filename="alliance-clients.csv"'}})}return NextResponse.json(clients)}
